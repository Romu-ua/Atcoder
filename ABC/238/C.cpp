#include <bits/stdc++.h>
using namespace std;

int main() {
    // 単に足していくだけだと間に合わない。
    string s; 
    cin >> s;
    int len = (int)s.length();

    int sum = 0;
    if (len > 1) {
        for (int i = 2; i <= len; i++) {
            // 桁数がN-1までの和を計算する
            
        }
    }
    // １桁の時はf(9)までの和が特殊なので別で処理する
    sum += 55;






    
    return 0;
}